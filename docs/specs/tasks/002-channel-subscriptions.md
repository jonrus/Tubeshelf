# Tasks: Channel Subscriptions
Spec: docs/specs/002-channel-subscriptions.md
Generated: 2026-07-22

- [x] 1. Add the `fast-xml-parser` runtime dependency — done when: `bun add fast-xml-parser` has been run and `"fast-xml-parser"` appears under `dependencies` in `package.json`.
- [x] 2. In `src/db/schema.ts`, remove the `channels` table and add `youtubeChannels` and `subscriptions` per the spec's Design > Schema section (including the `subscriptions_user_channel_unique` composite unique constraint), and retarget `videos.channelId`'s `.references()` from `channels.id` to `youtubeChannels.id` — done when: `schema.ts` exports `youtubeChannels` and `subscriptions` matching the spec, no `channels` export remains, `videos.channelId` references `youtubeChannels.id`, and `bun run lint` is clean.
- [x] 3. Generate the migration with `bun run db:generate`, then open the new file under `drizzle/` and confirm it drops/replaces the old `channels` table, creates `youtube_channels` and `subscriptions` (with the composite unique constraint actually present in the SQL — resolves the spec's Open Question about `unique(name).on(...)` codegen), and recreates `videos` with its FK pointed at `youtube_channels` (resolves the spec's Open Question about SQLite requiring a table-recreate for FK retargeting) — done when: the migration file exists with SQL matching those three expectations, and `bun test` still passes (the existing `test/smoke.test.ts` runs `migrate()` against a fresh in-memory DB).
- [x] 4. Create `src/lib/channel-input.ts` implementing `parseChannelInput` exactly per the spec's Input parsing section (raw channel ID, `/channel/<id>` URL, RSS URL with a `channel_id` query param; `null` for anything else) — done when: the file exports `parseChannelInput` matching the spec and `bun run lint` is clean.
- [x] 5. Write `test/lib/channel-input.test.ts` covering all three valid input forms, invalid input (`null` result), and confirming that the ID/`/channel/` URL/RSS URL forms for the *same* channel all normalize to an identical `{ channelId, rssUrl }` — done when: `bun test test/lib/channel-input.test.ts` passes.
- [x] 6. Create `src/lib/rss.ts` implementing `fetchChannelTitle` per the spec's RSS title lookup section, using `fast-xml-parser`'s `XMLParser` and a 5s timeout via `AbortSignal.timeout` — done when: the file exports `fetchChannelTitle` matching the spec and `bun run lint` is clean.
- [x] 7. Write `test/lib/rss.test.ts` for `fetchChannelTitle`, mocking `fetch` (resolve the spec's Open Question here — try `spyOn(globalThis, "fetch")` first and fall back to `mock.module` if it doesn't intercept a bare `fetch()` call under the installed Bun version) — cover success (parses `feed.title` from Atom XML), network error, timeout, non-OK response, and missing-title, all collapsing to `null` — done when: `bun test test/lib/rss.test.ts` passes with no real network call made.
- [x] 8. Create `src/lib/subscribe.ts` with `upsertYoutubeChannel(channelId, rssUrl)` and `upsertSubscription(userId, youtubeChannelId, categoryId)` per the spec's Subscribe flow steps 3–4, including the unique-constraint race recovery (on insert conflict, re-query and return/act on the existing row instead of throwing) — done when: both functions are exported matching the spec's described behavior and `bun run lint` is clean.
- [x] 9. Write `test/lib/subscribe.test.ts` exercising the race-recovery paths directly against `upsertYoutubeChannel` and `upsertSubscription` in isolation (pre-insert a conflicting row against an in-memory DB, call the function, assert it recovers by re-querying rather than throwing) — done when: `bun test test/lib/subscribe.test.ts` passes.
- [x] 10. Implement `src/routes/channels.tsx` with `GET /channels` (full page), `POST /subscriptions`, and `DELETE /subscriptions/:id`, per the spec's Subscribe flow (categoryId resolution incl. rejecting the system category, calling into `src/lib/subscribe.ts`) and Unsubscribe flow (ownership-scoped soft delete via `WHERE id = :id AND userId = currentUserId`, 404 on mismatch) sections, using the same deterministic default-user lookup as the spec's MVP note (`db.select().from(users).where(eq(users.username, "default")).get()`) — done when: the route file exists with handlers matching the spec's described request/response behavior for all listed cases (inline parse error, already-subscribed error, system-category rejection, reactivate-on-resubscribe, 404 on invalid/unauthorized unsubscribe).
- [x] 11. Create `src/views/channels-page.tsx` and `src/views/subscription-list.tsx`, mirroring the existing `categories-page.tsx`/`categories-list.tsx` pattern — subscribe form (text input + `<select>` of non-system categories with a blank `value=""` "Uncategorized" default option) posting via `hx-post="/subscriptions" hx-target="#subscription-list" hx-swap="outerHTML"`, and the active-subscriptions list (channel name, category, per-row `hx-delete="/subscriptions/{id}"` unsubscribe button) sorted alphabetically by channel name — done when: the views render correctly from `src/routes/channels.tsx`'s handlers.
- [x] 12. Wire the new route into `src/index.ts` (`app.route("/", channelsRoute)` alongside the existing `categoriesRoute`) — done when: `GET /channels` is reachable when the app is running (verify with `bun run dev:server` + `curl localhost:3000/channels`).
- [x] 13. Write `test/routes/channels.test.ts` covering: the subscribe→unsubscribe→resubscribe cycle against an in-memory DB with `fetch` mocked (same mechanism resolved in task 7); category resolution (blank `categoryId` lands on the system category, an explicit system-category id is rejected inline); unsubscribe ownership scoping (insert a second `users` row directly, create a subscription owned by it, assert `DELETE /subscriptions/:id` from the default user's context 404s and leaves that row untouched); and unsubscribe never touching `videos` (manually insert a `videos` row for a subscribed channel, unsubscribe, assert the row is unchanged and still present) — done when: `bun test test/routes/channels.test.ts` passes covering all four cases.
- [ ] 14. Run full verification: `bun test` and `bun run lint` clean across the whole repo — done when: both commands exit 0 with no failures.

Manual end-to-end verification (spec's "Verification" section, steps 1–8) is browser-driven and not part of this checklist — run it once all tasks above are checked off.
