# Tasks: Scheduled Video Ingestion
Spec: docs/specs/003-scheduled-video-ingestion.md
Generated: 2026-07-23

- [x] 1. In `src/db/schema.ts`, add nullable `lastFetchedAt` (`last_fetched_at`, timestamp mode) and `nextFetchDueAt` (`next_fetch_due_at`, timestamp mode) columns to `youtubeChannels`, per the spec's Design > Schema section — done when: both columns are exported on `youtubeChannels` matching the spec and `bun run lint` is clean.
- [x] 2. Generate the migration with `bun run db:generate`, then open the new file under `drizzle/` and confirm it's a plain `ALTER TABLE youtube_channels ADD COLUMN ...` for both new columns with no table recreation (per the spec, this is a simple additive change, unlike spec002's FK retarget — if `drizzle-kit` unexpectedly prompts interactively, per CLAUDE.md hand the exact command to the user to run in their own terminal rather than working around the missing TTY) — done when: the migration file exists with the expected `ALTER TABLE` statements and `bun test` still passes (`test/smoke.test.ts` runs `migrate()` against a fresh in-memory DB).
- [x] 3. In `src/lib/rss.ts`, replace `fetchChannelTitle` with `fetchChannelFeed(rssUrl): Promise<ChannelFeed | null>` per the spec's RSS feed parsing section — export `FeedEntry` (`videoId`, `title`, `description: string | null`, `publishedAt: Date`) and `ChannelFeed` (`title`, `entries: FeedEntry[]`) types; parse every `<entry>` (`id` → strip `yt:video:` prefix for `videoId`, `title`, `media:group`/`media:description` → nullable `description`, `published` → `publishedAt`); a malformed individual entry is logged and skipped rather than failing the whole fetch; all existing failure modes (network error, timeout, non-OK response, unparseable/missing title) still collapse to `null`, same 5s `AbortSignal.timeout` — done when: the file exports `fetchChannelFeed`/`FeedEntry`/`ChannelFeed` matching the spec, no `fetchChannelTitle` export remains, and `bun run lint` is clean. Per the spec's Open Questions, verify `fast-xml-parser`'s actual output shape for `<entry>`/`media:group`/`media:description`/the `yt:videoId`-bearing `<id>` element against a real feed fetch and adjust the parsing logic if it differs from what's sketched in the spec.
- [x] 4. Update `test/lib/rss.test.ts` for `fetchChannelFeed`: extend the existing Atom XML fixture with `<entry>` elements (including `media:group`/`media:description` and `yt:videoId`-style namespacing) and assert `.title` and `.entries` both parse correctly; add a case where one entry is malformed and is skipped without failing the whole fetch; carry over the existing network error/timeout/non-OK/missing-title cases (now asserting `fetchChannelFeed` returns `null`) — done when: `bun test test/lib/rss.test.ts` passes with no real network call made.
- [x] 5. In `src/lib/channel-input.ts`, export `rssUrlFor` (currently a private helper) and `CHANNEL_ID_PATTERN` (currently a private const) — done when: both are named exports, `parseChannelInput`'s behavior is unchanged, `test/lib/channel-input.test.ts` still passes, and `bun run lint` is clean.
- [x] 6. Create `src/lib/ingest.ts` with `applyFeedToChannel(channelId, feed)`, `ingestChannel(channel): Promise<{ ok: boolean }>`, and the internal `nextDueAt`/`safeReschedule` helpers, per the spec's Core ingestion section — `BASE_INTERVAL_MS` (1 hour) / `JITTER_MS` (±5 min); video upsert keyed on `youtubeVideoId` via `.onConflictDoUpdate` updating only `title`/`description`/`publishedAt` (never `status`/`ignoreMethod`); gap detection comparing the feed's oldest entry against the previously-newest stored video (`possibleMissedVideos` only ever set to `true`, never cleared, and not evaluated on a channel's first-ever ingest); `ingestChannel` never throws, reschedules `nextFetchDueAt` on both fetch failure and a thrown DB error from `applyFeedToChannel` — done when: `src/lib/ingest.ts` exports `applyFeedToChannel` and `ingestChannel` matching the spec's described behavior and `bun run lint` is clean. Per the spec's Open Questions, verify `.onConflictDoUpdate({ target, set })` compiles and behaves as expected against the installed `drizzle-orm@0.45.2` and adjust if the real API differs.
- [x] 7. Write `test/lib/ingest.test.ts` against an in-memory DB per the spec's Testing section: `applyFeedToChannel` — new videos inserted as `unwatched`; re-ingesting an existing video updates title/description/publishedAt without touching status/ignoreMethod; gap detection fires when the feed's oldest entry postdates the previously-newest stored video, does not fire on a channel's first-ever ingest, and does not clear an already-true flag on a subsequent gap-free ingest. `ingestChannel` — a failed fetch (mock `fetchChannelFeed`/`fetch` to return null) still advances `nextFetchDueAt`; a DB-layer error thrown from inside `applyFeedToChannel` (mock it to throw) is caught, still advances `nextFetchDueAt`, and `ingestChannel` resolves to `{ ok: false }` rather than rejecting. `safeReschedule` — a failing update (mock the DB call to throw) is caught/logged rather than propagating — done when: `bun test test/lib/ingest.test.ts` passes covering all listed cases.
- [x] 8. In `src/lib/subscribe.ts`, switch `upsertYoutubeChannel` from `fetchChannelTitle` to `fetchChannelFeed`, and change its return type to `Promise<{ channel: YoutubeChannelRow; feed: ChannelFeed | null } | null>` per the spec's step 3 under Subscribe flow: `feed` is the fetched `ChannelFeed` when the function inserted a brand-new row, `null` when it found an existing row immediately or lost the insert race (its own fetched feed discarded in that case); overall `null` return unchanged (fetch failed for a genuinely new channel) — done when: `upsertYoutubeChannel`'s new signature matches the spec, the existing race-recovery logic is otherwise unchanged, and `bun run lint` is clean (this will produce compile errors in `src/routes/channels.tsx` until task 12 updates its caller — that's expected at this point).
- [x] 9. Update `test/lib/subscribe.test.ts` for `upsertYoutubeChannel`'s new return shape per the spec's Testing section: a brand-new channel returns `{ channel, feed }` with `feed` populated from the fetch it just made; an already-existing channel returns `{ channel, feed: null }` with no fetch attempted; the existing race-recovery test (pre-insert a conflicting row, call the function, assert it recovers) still passes and now asserts `feed: null` on that path — done when: `bun test test/lib/subscribe.test.ts` passes.
- [x] 10. Create `src/lib/scheduler.ts` with `dueChannels(now, limit)`, `tick()`, `runGuardedTick()`, and `startScheduler()` per the spec's Scheduler section — `TICK_INTERVAL_MS` (1 minute) / `BATCH_SIZE` (5); `dueChannels` selects only channels with ≥1 active (`unsubscribedAt IS NULL`) subscription and a null or past-due `nextFetchDueAt`, ordered oldest-overdue-first; `tick` awaits `ingestChannel` sequentially for each due channel; `runGuardedTick` skips re-entrantly if a previous tick is still in flight (module-level `ticking` flag) and catches/logs any error from `tick`; `startScheduler` wraps `runGuardedTick` in a `setInterval` — done when: `src/lib/scheduler.ts` exports all four functions matching the spec and `bun run lint` is clean. Per the spec's Open Questions, verify comparing a `timestamp`-mode column against a plain JS `Date` inside `lte(...)`/`or(isNull(...), ...)` compiles and filters as expected against the installed `drizzle-orm` version, and adjust if it doesn't.
- [x] 11. Write `test/lib/scheduler.test.ts` per the spec's Testing section: `dueChannels` — only returns channels with ≥1 active subscription, excludes a future `nextFetchDueAt`, includes a null `nextFetchDueAt`, respects the batch limit, and orders oldest-overdue-first. `runGuardedTick`'s re-entrancy guard — calling it a second time while a mocked `tick()` is still pending is a no-op; a subsequent call after the pending one resolves runs normally; the test must let its mocked `tick()` actually resolve/reject before the test ends so the shared `ticking` flag doesn't leak `true` into later tests in the file — done when: `bun test test/lib/scheduler.test.ts` passes covering all listed cases.
- [x] 12. Split `POST /subscriptions` in `src/routes/channels.tsx` into `POST /subscriptions/preview` and `POST /subscriptions` (confirm), per the spec's "Subscribe flow becomes preview -> confirm" section: preview re-uses `parseChannelInput` + category validation, looks up `youtube_channels` by `channelId` (existing row → use stored name, no fetch; not found → `fetchChannelFeed(rssUrl)`, `.title`, null → inline error) and renders a confirmation partial carrying `channelId`+`categoryId` as hidden fields (never `rssUrl`); confirm re-validates `categoryId` and `channelId` (against the now-exported `CHANNEL_ID_PATTERN`), derives `rssUrl` server-side via `rssUrlFor(channelId)` (never trusts an `rssUrl` field), calls the now-updated `upsertYoutubeChannel(channelId, rssUrl)` and either `applyFeedToChannel(channel.id, feed)` when `feed` is populated or `ingestChannel(channel)` when `feed` is null (a failure from `ingestChannel` here does not block subscription creation), then `upsertSubscription` as before, responding with the blank subscribe form plus an out-of-band `#subscription-list` swap — done when: both routes exist in `src/routes/channels.tsx` matching the spec's described request/response behavior, and `bun run lint` is clean.
- [ ] 13. Update `src/views/channels-page.tsx` and add a confirmation partial view (e.g. `src/views/subscribe-confirm.tsx`) per the spec: the subscribe form now posts to `/subscriptions/preview` (targeting a container for the confirmation panel) instead of directly to `/subscriptions`; the new confirm partial renders the resolved channel name, hidden `channelId`/`categoryId` fields, a "Confirm Subscribe" button (`hx-post="/subscriptions"`), and a "Cancel" action that swaps back to the blank subscribe form — done when: the views render correctly from `src/routes/channels.tsx`'s preview/confirm handlers and the two-step flow is visually coherent (verify manually with `bun run dev:server` in a browser, per CLAUDE.md's container/curl notes for checking the running app from inside the container).
- [ ] 14. Update `test/routes/channels.test.ts` for the preview/confirm split per the spec's Testing section: preview renders a confirmation with the real fetched/stored name and writes nothing to any table; confirm creates the subscription *and* populates `videos` for that channel in one round trip; confirming against an already-known channel does not attempt a channel-title fetch (only the ingest fetch — assert via the mocked `fetch` call count); a confirm request with a mismatched/malformed `channelId` (simulating a hand-crafted request that skipped preview) is rejected inline rather than inserted; a confirm request cannot influence which URL the server fetches by anything other than `channelId` (assert the mocked `fetch`'s call argument always equals `rssUrlFor(channelId)` regardless of other body fields) — done when: `bun test test/routes/channels.test.ts` passes covering all listed cases, including the pre-existing subscribe→unsubscribe→resubscribe and category-resolution cases updated for the new two-step flow.
- [ ] 15. In `src/index.ts`, call `startScheduler()` once after `seed(db)` (alongside the existing `Bun.serve` call), per the spec's Scheduler section — done when: `startScheduler()` is invoked at module load and `bun run lint` is clean.
- [ ] 16. Run full verification: `bun test` and `bun run lint` clean across the whole repo — done when: both commands exit 0 with no failures.

Manual end-to-end verification (spec's "Verification" section, steps 1–4) is browser/DB-driven and not part of this checklist — run it once all tasks above are checked off.
