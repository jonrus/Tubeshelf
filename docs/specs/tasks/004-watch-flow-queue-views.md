# Tasks: Watch Flow & Queue Views
Spec: docs/specs/004-watch-flow-queue-views.md
Generated: 2026-07-24

- [x] 1. In `src/db/schema.ts`, add a nullable `watchedAt` (`watched_at`, timestamp mode) column to `videos` plus a `watched_at_check` CHECK constraint tying it to `status`, per the spec's Design > Schema addition section (`(status = 'watched') = (watched_at is not null)`) — done when: `watchedAt` is exported on `videos`, the new check is added alongside the existing `status_check`/`ignore_method_check` in the same `(t) => [...]` array, and `bun run lint` is clean.
- [x] 2. Generate the migration with `bun run db:generate`, then inspect the new file under `drizzle/` and confirm it recreates the `videos` table (create-new/copy-rows/drop-old/rename) rather than a plain `ALTER TABLE ADD COLUMN` — per the spec, this is expected since `watched_at_check` references the pre-existing `status` column, unlike spec003's additive columns. If `drizzle-kit` prompts interactively (the "rename vs. new table" disambiguation spec002 hit), per CLAUDE.md hand the exact command to the user to run in their own terminal rather than attempting a workaround — done when: the migration file exists with the expected table-recreation SQL and `bun test` still passes (`test/smoke.test.ts` runs `migrate()` against a fresh in-memory DB).
- [x] 3. Create `src/lib/current-user.ts` exporting `getCurrentUser()` — move the existing private implementation out of `src/routes/channels.tsx` (`channels.tsx:52-60`) unchanged (lookup by seeded `"default"` username, throws if missing), then update `channels.tsx` to import it from there and delete the now-duplicate local function — done when: `getCurrentUser` is only defined in `src/lib/current-user.ts`, `channels.tsx` imports and uses it, `bun test test/routes/channels.test.ts` still passes, and `bun run lint` is clean.
- [x] 4. Create `src/lib/watch-status.ts` exporting `setWatching(videoId)`, `toggleQueueStatus(videoId)`, and `toggleWatchedFromWatchingPage(videoId)` per the spec's Design > Watch status transitions section, matching the code sketched there exactly (including the comments explaining why `setWatching` only conditionally touches `watchedAt`, and why the other two functions have different transition tables for the `watching` starting state) — done when: all three functions are exported, each returns `null` for a nonexistent video ID, and `bun run lint` is clean.
- [x] 5. Write `test/lib/watch-status.test.ts` against an in-memory DB per the spec's Testing section: `setWatching` — unwatched/watching/watched all transition to `watching`; returns `null` for a nonexistent video ID; transitioning from `watched` clears `watchedAt` to `null`; transitioning from `unwatched`/`watching` leaves `watchedAt` untouched (already `null`). `toggleQueueStatus` — unwatched→watched (sets `watchedAt` to a non-null recent timestamp), watched→unwatched (clears `watchedAt`), watching→unwatched (`watchedAt` stays `null`); returns `null` for a nonexistent video ID. `toggleWatchedFromWatchingPage` — unwatched→watched **and** watching→watched (both set `watchedAt` — assert the `watching` case explicitly, since this is the regression the spec's Context section describes), watched→unwatched (clears `watchedAt`); returns `null` for a nonexistent video ID — done when: `bun test test/lib/watch-status.test.ts` passes covering all listed cases.
- [x] 6. Create `src/routes/queue.tsx` with the query helpers `queueVideos(userId, sort)`, `continueWatchingVideos(userId)`, and `watchedVideos(userId)` per the spec's Design > Queue queries section (all three joining `videos` → `youtubeChannels` → `subscriptions` → `categories`; `queueVideos`/`continueWatchingVideos` scoped to `isNull(subscriptions.unsubscribedAt)`, `watchedVideos` deliberately not scoped that way so unsubscribed channels' watched history still appears) — also add `resolveToggleView(view)` and `resolveReturnTarget(from, sort)` (with the `RETURN_VIEWS` map) exactly per the spec's Design > Routes and Return-to-origin navigation sections — done when: all five are defined in the file, `bun run lint` is clean, and per the spec's Open Questions, `inArray(videos.status, [...])` against the installed `drizzle-orm@0.45.2` is confirmed to compile and filter as expected (adjust the query shape if it doesn't).
- [x] 7. Create the queue list view `src/views/queue-list.tsx` per the spec's Design > Queue queries and Click-to-watch sections: renders a list of video rows for a given view (`queue`/`continue-watching`/`watched`), each title as a plain anchor to `/watching/:id?from=<view>[&sort=<sort>]` carrying `data-youtube-url` (not an inline `onclick`), a toggle button on `queue`/`continue-watching` rows only (labeled "Mark Watched" for unwatched rows, "Clear to Unwatched" for watching rows, `hx-post="/videos/:id/toggle?view=...&sort=..."` with `hx-disabled-elt="this"`), no toggle button on `watched` rows (click-through only) — done when: the component renders all three view shapes correctly and `bun run lint` is clean.
- [ ] 8. Create the Watching page view `src/views/watching-page.tsx` per the spec's Design > Watching page section: thumbnail from `https://i.ytimg.com/vi/{youtubeVideoId}/hqdefault.jpg`; the `hx-trigger="load delay:10s" hx-post="/videos/:id/watching" hx-swap="none"` auto-timeout element rendered only when status !== "watched"; the `<span id="watch-status-badge" hx-swap-oob="true">` badge; three actions (Mark Watching button, Mark Watched/Unwatched & Return to `{returnLabel}` form with submit-disable `onsubmit`, plain Return to `{returnLabel}` link) all computed via `resolveReturnTarget(from, sort)`; the bfcache `pageshow`/`persisted` → `location.reload()` inline `<script>` scoped to this view only (not `layout.tsx`) — done when: the component renders correctly for both a `watched` and a non-`watched` video and `bun run lint` is clean.
- [ ] 9. In `src/routes/queue.tsx`, add the five routes per the spec's Design > Routes section, rendering the real views from tasks 7/8 directly (no placeholder needed — both views already exist): `GET /queue?sort=newest|oldest`, `GET /continue-watching`, `GET /watched`, `GET /watching/:id?from=...&sort=...` (404 if the video doesn't exist, no subscription-active check), and the three POST routes (`POST /videos/:id/watching`, `POST /videos/:id/watched-toggle?from=...&sort=...`, `POST /videos/:id/toggle?view=...&sort=...`) wired to `setWatching`/`toggleWatchedFromWatchingPage`/`toggleQueueStatus` respectively, using `getCurrentUser()` from `src/lib/current-user.ts`; register the route module (e.g. exported as `queueRoute`) in `src/index.ts` via `app.route("/", queueRoute)` alongside the existing `categoriesRoute`/`channelsRoute` — done when: hitting `/queue`, `/continue-watching`, `/watched`, and `/watching/:id` in a running dev server returns real rendered HTML, and `bun run lint` is clean.
- [ ] 10. Add the click-to-watch listener (`handleWatchLinkClick`, handling `click`/`auxclick`, ignoring modifier-clicks and non-primary `auxclick` buttons per the spec's Design > Click-to-watch section) as an inline `<script>` in `src/views/layout.tsx`, and add the minimal top nav (`Categories` / `Channels` / `Queue` / `Continue Watching` / `Watched`, no active-link highlighting) per the spec's Design > Nav section — done when: the nav links are present on every page and the click-to-watch script is loaded sitewide via `layout.tsx`, and `bun run lint` is clean.
- [ ] 11. Write `test/routes/queue.test.ts` per the spec's Testing section, covering: `GET /queue` (only unwatched/watching for active subscriptions, excludes an unsubscribed channel's videos, excludes watched videos, default newest-first, `?sort=oldest` inverts, row links carry `from=queue`+current sort); `GET /continue-watching` (only watching, same active scoping, `from=continue-watching`); `GET /watched` (only watched, most-recently-watched-first, **still includes** a video whose channel was since unsubscribed, `from=watched`); `GET /watching/:id` (404 for nonexistent, correct Mark Watched/Unwatched label, auto-timer element present/absent by status, all `from` values plus missing/unrecognized fallback to `/queue`, `sort` round-trip); `POST /videos/:id/watching` (always sets `watching`); `POST /videos/:id/watched-toggle` (the `watching`→`watched` regression case explicitly, watched→unwatched, 303 redirect target for all `from` values plus fallback); `POST /videos/:id/toggle` (status change per `toggleQueueStatus`, partial reflects row removal from its view, `view` fallback to `queue`) — done when: `bun test test/routes/queue.test.ts` passes covering all listed cases.
- [ ] 12. Run full verification: `bun test` and `bun run lint` clean across the whole repo — done when: both commands exit 0 with no failures.

Manual end-to-end verification (spec's "Verification" section, steps 1–16, including the three client-side-only behaviors with no automated coverage: bfcache reload, auxclick/modifier-key handling, and double-submit guards) is browser/DB-driven and not part of this checklist — run it once all tasks above are checked off.
